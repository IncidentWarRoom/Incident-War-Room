server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routes (e.g. /incidents/abc), so any
    # path that isn't a real file should fall back to index.html.
    location / {
        try_files $uri /index.html;
    }
}
